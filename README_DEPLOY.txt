TILLMAN TOUGH — FULLY LOCAL CLIENT PREVIEW V4

This package is already built. Do not run npm install or npm run build.
All runtime image files are included under assets/images/.
There are no Wix image URLs and no image proxy endpoint.
Deploy the folder contents directly to GitHub/Vercel.

Open /review for the guided client presentation.
